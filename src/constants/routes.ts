import { RelativePathString } from 'expo-router'

export const PATH = {
  settings: '/settings' as RelativePathString,
  buscaRapida: '/busca-rapida' as RelativePathString,
  metronomo: '/metronomo' as RelativePathString,
}
